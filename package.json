{
  "name": "zolocke-github-io",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/ensure-source-index.mjs && vite",
    "build": "node scripts/ensure-source-index.mjs && tsc && vite build",
    "preview": "vite preview",
    "deploy:prepare": "npm run build && node scripts/prepare-pages.mjs"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
